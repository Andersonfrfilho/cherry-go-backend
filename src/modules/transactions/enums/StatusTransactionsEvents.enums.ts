export enum StatusTransactionsEnum {
  PROGRESS = "progress",
  FINISHED = "finished",
  CANCELED = "canceled",
  REFUNDS = "refunds",
}

export enum StatusEventsTransactionsEnum {
  PAYMENT = "payment",
  REFUND = "refund",
  PARTIAL = "partial",
}
