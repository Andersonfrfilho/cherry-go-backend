export enum StatusTransactions {
  PROGRESS = "progress",
  FINISHED = "finished",
  CANCELED = "canceled",
  REFUNDS = "refunds",
}

export enum StatusEventsTransactions {
  PAYMENT = "payment",
  REFUND = "refund",
  PARTIAL = "partial",
}
